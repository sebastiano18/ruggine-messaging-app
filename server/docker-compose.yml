services:
  server:
    build: .
    container_name: server
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      # Persiste il database SQLite fuori dal container
      - ./data:/app/data
    env_file:
      - .env
